{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug starmap (CMake)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\build\\bin\\starmap.exe",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "CMake Build"
    }
  ]
}

